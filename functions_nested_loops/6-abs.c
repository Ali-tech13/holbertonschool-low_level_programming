#include "main.h"
#include <stdio.h>
/**
* _abs - calculate the abs value of an intger
* @n: number to check
*
* Return: Abs value of n
**/
int _abs(int n)
{
if (n < 0)
{
return (-n);
}
return (n);
}
