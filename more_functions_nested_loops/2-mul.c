#include "main.h"
/**
* mul - function that multiplies two integers.
* @a: number to check
* @b: number to check
* Return: return result
**/
int mul(int a, int b)
{
int result;
result = a *b;
return (result);
}